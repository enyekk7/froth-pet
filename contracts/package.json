{
  "name": "kittycraft-contracts",
  "version": "1.0.0",
  "description": "KittyCraft Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network flowEVM",
    "deploy:full": "hardhat run scripts/full-deploy-and-test.js --network flowEVM",
    "test:mint": "hardhat run scripts/test-mint.js --network flowEVM",
    "check:balance": "hardhat run scripts/check-balance.js --network flowEVM",
    "devload": "hardhat run scripts/devload-nft.js --network flowEVM",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "axios": "^1.13.1"
  }
}
